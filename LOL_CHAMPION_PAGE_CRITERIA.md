# 롤 챔피언 페이지 작업 기준

이 문서는 롤 챔피언 웹페이지와 비슷한 화면을 다시 만들거나 수정할 때 참고할 기준만 정리한 문서입니다.

## 데이터 연동 기준

- 챔피언 데이터는 Riot Data Dragon API를 사용합니다.
- 먼저 최신 버전을 조회한 뒤, 해당 버전의 한국어 챔피언 데이터를 요청합니다.

```text
https://ddragon.leagueoflegends.com/api/versions.json
https://ddragon.leagueoflegends.com/cdn/{version}/data/ko_KR/champion.json
```

- 챔피언 이미지는 같은 버전의 Data Dragon 이미지 경로를 사용합니다.
- API 호출에는 로딩 상태와 오류 상태를 둡니다.
- 빈 화면보다 현재 상태가 명확히 보이는 UI를 우선합니다.

## 챔피언 카드 기준

- 카드에는 챔피언 초상화, 이름, 칭호, 역할 태그를 표시합니다.
- 역할 태그는 API 원본 영어 값을 화면 표시용 한글 값으로 매핑합니다.

```text
Fighter  -> 전사
Tank     -> 탱커
Mage     -> 마법사
Assassin -> 암살자
Marksman -> 원거리 딜러
Support  -> 서포터
```

- 검색 대상에는 한글 이름, 영문 id, 칭호, 영어 역할명, 한글 역할명을 모두 포함합니다.
- 사용자가 `Ahri`, `아리`, `마법사`처럼 서로 다른 방식으로 검색해도 결과가 나와야 합니다.

## 역할군 필터 기준

- 역할군 필터는 다중 선택 가능한 콤보박스 형태를 사용합니다.
- 선택하지 않은 상태는 `전체`로 취급합니다.
- 여러 역할군을 선택했을 때는 AND 조건으로 필터링합니다.

```text
마법사 + 서포터 선택
= 마법사와 서포터 역할을 모두 가진 챔피언만 표시
```

- 검색어와 역할군 필터는 동시에 적용합니다.
- 역할군 필터는 검색창보다 보조 기능이므로 PC에서는 툴바 오른쪽에 배치합니다.

## PC 레이아웃 기준

- PC 화면에서는 챔피언 카드가 한 줄에 10명씩 보이도록 합니다.
- 15명 배치는 카드가 너무 작아져 이름과 역할 태그의 가독성이 떨어질 수 있습니다.
- 10명 배치는 많은 챔피언을 보여주면서도 카드 정보가 읽히는 균형점으로 봅니다.
- 검색창은 왼쪽에 유지합니다.
- 역할군 필터는 툴바 오른쪽 끝에 배치합니다.

## 모바일 헤더 기준

- 왼쪽에는 `LEAGUE OF LEGENDS`, `챔피언 목록`, 설명 문구를 하나의 정보 묶음으로 둡니다.
- 오른쪽에는 버전/챔피언 수 통계 박스를 둡니다.
- 왼쪽 정보 묶음의 세로 여백은 최소화합니다.
- 통계 박스의 기존 형식은 유지합니다.
- 통계 박스 옆 구분선은 사용하지 않습니다.
- 설명 문구는 모바일에서 두 줄로 표현되도록 합니다.
- 모바일 수정은 되도록 `@media (max-width: 720px)` 안에서 처리해 PC 화면에 영향이 가지 않도록 합니다.

## 통계 박스 기준

- `Champions` 문구는 `챔피언`으로 표시합니다.
- 박스는 내용 크기에 맞게 줄입니다.
- 내부 텍스트는 가운데 정렬합니다.
- 통계 박스는 정보 강조 요소이므로 너비를 과하게 늘리지 않습니다.
- 모바일에서도 전체 폭으로 늘어나지 않게 유지합니다.

## 모바일 툴바 기준

- 모바일에서는 검색창과 역할군 필터를 세로로 배치합니다.
- 좁은 화면에서 두 컨트롤을 한 줄에 억지로 넣지 않습니다.
- 역할군 드롭다운 메뉴는 모바일 화면 폭에 맞게 펼쳐지도록 합니다.

## 스타일 기준

- 카드, 툴바, 헤더는 8px 이하의 둥근 모서리를 사용합니다.
- 전체 톤은 롤 클라이언트 느낌에 맞춰 어두운 배경, 금색 강조, 청록색 태그를 사용합니다.
- 금색은 주요 강조와 테두리에 사용합니다.
- 청록색은 역할 태그처럼 보조 분류 정보에 사용합니다.
- 장식 요소는 최소화하고 실제 정보 구조가 먼저 보이게 합니다.
- 색상은 한 가지 계열로만 치우치지 않게 합니다.

## 검증 기준

비슷한 작업을 다시 할 때는 아래 순서로 확인합니다.

1. `npm.cmd run build` 실행
2. 브라우저에서 전체 챔피언 수가 정상 표시되는지 확인
3. 검색어 입력 후 결과가 줄어드는지 확인
4. 역할군을 여러 개 선택했을 때 AND 조건으로 필터링되는지 확인
5. PC 폭에서 한 줄에 카드 10개가 보이는지 확인
6. 모바일 폭에서 헤더가 깨지지 않는지 확인
7. 콘솔 에러가 없는지 확인

## 파일 책임 기준

- `src/App.vue`
  - API 호출
  - 챔피언 데이터 상태 관리
  - 검색
  - 역할군 다중 선택 필터
  - 화면 렌더링

- `src/style.css`
  - 전체 색상과 레이아웃
  - 챔피언 카드 그리드
  - 검색/필터 툴바
  - PC/모바일 반응형 헤더
  - 통계 박스 스타일

